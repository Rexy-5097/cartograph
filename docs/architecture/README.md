# Architecture detail

Primary document: [ARCHITECTURE.md](../../ARCHITECTURE.md). This directory
holds deep-dives as subsystems land: semantic model (M04), storage layout
(M09/M10), incremental invalidation (M10), layout engine (M11). At M00 the
implemented architecture is fully covered by the primary document.
