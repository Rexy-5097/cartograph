# Milestones

One file per milestone: scope, acceptance criteria, gates, exit artifacts.
A milestone is complete when its gate passes and the human owner accepts the
PR — never merely when code exists (RULES 016–018, 025).

Timeline and stopping gates: [../../ROADMAP.md](../../ROADMAP.md).
State: [../artifacts/project-state.yaml](../artifacts/project-state.yaml).
