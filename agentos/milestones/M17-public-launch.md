# M17 — Public launch and technical writeup

Target: Week 25 · Branch: `feature/m17-public-launch` · Tag on acceptance: `cartograph-m17`

Scope: v1.0 stable, technical writeup (resolver design, benchmark results incl.
failures, calibration), docs site (VitePress).

Acceptance: writeup published; every number in it traceable to a benchmark run;
gates pass.

Standard exit: PR + CI green + self-review + human acceptance + checkpoint tag + state update + STOP.
