# M16 — ASK — explanation over subgraph evidence

Target: Week 24 · Branch: `feature/m16-ask` · Tag on acceptance: `cartograph-m16`

Scope: opt-in, per-repository AI explanation over already-derived subgraph
evidence. The model interprets; it never constructs (RULES 007/008). Keys in OS
keychain; fully functional product without any key.

Acceptance: ASK answers cite edge evidence verbatim; with AI disabled the
feature degrades to showing the raw evidence; gates pass.

Standard exit: PR + CI green + self-review + human acceptance + checkpoint tag + state update + STOP.
