# QG-008 — Milestone acceptance

The active milestone's acceptance criteria (its file in `../milestones/`) are
all satisfied, QG-001…007 all PASS, and no work from a future milestone has
been smuggled in (verified by diff review — e.g. at M00 the parser and resolver
crates must still be empty).

Human acceptance of the PR — not this gate — creates the checkpoint tag.
