# QG-001 — Repository integrity

- Workspace `Cargo.toml` lists exactly the six crates and each exists.
- Required governance files present: README, LICENSE, CHANGELOG, CONTRIBUTING,
  SECURITY, CODE_OF_CONDUCT, AGENTS, AGENTOS, PROJECT_RULES, ARCHITECTURE,
  ROADMAP, CHECKPOINTS, Makefile, .gitignore, .gitattributes.
- AgentOS present with PROJECT_CONFIG, state file, milestones, gates.
- `.github/` has CI workflow, PR template, issue templates, CODEOWNERS.
- No `._*` AppleDouble files tracked by git.
