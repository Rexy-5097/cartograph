// Fixture: a broken region must not destroy the rest of the file.
import { good } from "./good";

function fine(): void {}

function broken( {   // unclosed parameter list

export function alsoFine(): void {}
const AFTER = "/still/extracted";
